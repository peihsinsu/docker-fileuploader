{
  "name": "file-uploadir",
  "version": "1.0.0",
  "description": "## Basic File Upload **Your node.js code:** ```javascript const express = require('express'); const fileUpload = require('express-fileupload'); const app = express();",
  "main": "server.js",
  "dependencies": {
    "e": "0.0.4",
    "express": "^4.16.4",
    "express-fileupload": "^1.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
